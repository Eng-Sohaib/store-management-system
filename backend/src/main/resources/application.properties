# Change the web server port to avoid conflicts
server.port=8081

# Database Connection Settings (Updated URL)
spring.datasource.url=jdbc:mysql://localhost:3306/store_management?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=So2177250
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true